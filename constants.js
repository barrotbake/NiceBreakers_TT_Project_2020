// WebSocket Event Constants
export const NEXT_ROUND = "next-round"; // -> Client
export const ANSWER_QUESTION = "answer-question"; // -> Server
export const CREATE_LOBBY = "create-lobby"; // -> Server
export const JOIN_LOBBY = "join-lobby"; // -> Server
export const SET_INFO = "set-info"; // -> Server; Sets the name, prefered pronoun, and name pronunciation of a user
export const ROUND_TYPES = {
  TWO_TRUTHS_AND_A_LIE: "Two Truths and a Lie",
};

// Miscellaneous Constants
export const HOST = "localhost:3000";
